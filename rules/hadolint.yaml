source:
  github:
    repository: hadolint/hadolint
    version: "2.14.0"
    release_slug: "v{version}/{name}-{target}"
    targets:
      - macos-arm64
      - macos-x86_64
      - linux-arm64
      - linux-x86_64

targets:
  - pypi: {}
