[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pybin"
version = "0.0.1"
requires-python = ">=3.10"

[tool.setuptools]
zip-safe = false

[tool.setuptools.packages.find]
where = ["src"]
